{
  "name": "quiz-app",
  "version": "1.0.0",
  "description": "Quiz Application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "db:setup": "node database/migrate.js full",
    "db:schema": "node database/migrate.js schema",
    "db:migrate": "node database/migrate.js migrate",
    "db:test": "node -e \"require('./config/database').testConnection()\""
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "express": "^4.18.2",
    "socket.io": "^4.8.3",
    "mssql": "^10.0.2",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
